# Database
POSTGRES_USER=cleandrop
POSTGRES_PASSWORD=cleandrop
POSTGRES_DB=cleandrop
DATABASE_URL=postgres://cleandrop:cleandrop@db:5432/cleandrop

# Host-side port mappings (override if the defaults clash with other services)
POSTGRES_HOST_PORT=5433
API_HOST_PORT=3000
WEB_HOST_PORT=8080

# API
NODE_ENV=development
JWT_SECRET=change-me-in-production-this-is-a-dev-secret-not-for-real-use

# Web (build-time)
VITE_API_URL=http://localhost:3000
